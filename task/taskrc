# Use the command 'task show' to see all defaults and overrides

# Aliases
alias.a=add

# Files
data.location=~/.task

# Custom colors
color.blocked=black
color.tagged=
color.tag.today=bright yellow
color.tag.!epic=black
color.due=green
color.due.today=bright yellow
color.overdue=red

# Misc changes
blanklines=false
due=3
search.case.sensitive=no
regex=true

# Reports overrides
report.list.columns=id,project,due,tags,description
report.list.labels=ID,Project,Due,Tags,Description
report.list.filter=status:pending -someday
report.list.sort=due+,project+,description+

# Custom reports
report.someday.columns=id,project,age,tags,description                                                      
report.someday.description=Lists tasks to be completed someday.
report.someday.filter=status:pending +someday
report.someday.labels=ID,Project,Age,Tags,Description                                                      
report.someday.sort=age-,project+,description+
